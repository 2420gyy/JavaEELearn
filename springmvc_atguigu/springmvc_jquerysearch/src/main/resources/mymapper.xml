<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.dao.QueryDao">
    <select id="queryProvinceList" resultType="com.chen.pojo.Province">
        select id, name, jiancheng, shenhui from ssmbuild.provice;
    </select>
</mapper>